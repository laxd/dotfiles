[core]
align = center
display_modules = distro(), uname(n), uname(r), uptime(), ram(), cpu(), env(editor), fs(/), fs(/home) mpd(albums)
color = blue

[fs]
low_bound = 80

[sensor]
sensor = coretemp-*
